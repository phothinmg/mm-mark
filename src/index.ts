import { Mmmark } from "./converter.js";
import type {
	DataProps,
	FrontMatter,
	MmmarkOptions,
	RenderOptions,
} from "./types.js";

export type { DataProps, FrontMatter, MmmarkOptions, RenderOptions };

export default Mmmark;
