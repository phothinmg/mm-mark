import type {
  DataProps,
  FrontMatter,
  MmmarkOptions,
  RenderOptions,
} from "./types";
import { Mmmark } from "./converter";

export type { DataProps, FrontMatter, MmmarkOptions, RenderOptions };

export default Mmmark;
